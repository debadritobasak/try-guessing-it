import random
import string

def validate_length():
    while True:
        try:
            length = int(input("Enter password length: "))
            if length <= 0:
                print("Length must be greater than 0.")
            else:
                return length
        except ValueError:
            print("Invalid input! Enter a number.")

def generate_password(length, upper, number, special):
    chars = string.ascii_lowercase

    if upper == "yes":
        chars += string.ascii_uppercase
    if number == "yes":
        chars += string.digits
    if special == "yes":
        chars += string.punctuation

    password = ""
    for i in range(length):
        password += random.choice(chars)

    return password

def display_password(password):
    print("\nGenerated Password:")
    print(password)

# Main Program
length = validate_length()

upper = input("Include uppercase letters? (yes/no): ").lower()
number = input("Include numbers? (yes/no): ").lower()
special = input("Include special characters? (yes/no): ").lower()

password = generate_password(length, upper, number, special)
display_password(password)